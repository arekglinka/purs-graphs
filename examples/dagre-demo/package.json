{
  "name": "dagre-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n SPAGO,VITE \"spago build --watch\" \"vite\"",
    "build": "spago build && purs-backend-es build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dagre": "0.8.5"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "vite": "^5.4.0"
  }
}
