{
  "name": "showcase",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n SPAGO,VITE \"spago build\" \"vite\"",
    "build": "spago build && purs-backend-es build --corefn-dir ../../output --output-dir ../../output-es && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "concurrently": "^9.0.0",
    "tailwindcss": "^4.3.3",
    "vite": "^5.4.0"
  }
}
